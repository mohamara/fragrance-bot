# راهنمای سریع راه‌اندازی

## مراحل سریع

### 1. نصب وابستگی‌ها
```bash
npm install
```

### 2. تنظیم متغیرهای محیطی
```bash
cp .env.example .env
```

سپس فایل `.env` را ویرایش کرده و مقادیر زیر را وارد کنید:
- `OPENAI_API_KEY`: کلید API از OpenAI
- `TELEGRAM_BOT_TOKEN`: توکن ربات از BotFather در تلگرام

### 3. افزودن فایل‌های پایگاه دانش
فایل‌های متنی خود را در پوشه `knowledge_base` قرار دهید.

### 4. اجرای ربات
```bash
npm start
```

## دریافت توکن ربات تلگرام

1. در تلگرام به [@BotFather](https://t.me/BotFather) پیام دهید
2. دستور `/newbot` را ارسال کنید
3. نام و username ربات را انتخاب کنید
4. توکن دریافتی را در فایل `.env` قرار دهید

## دریافت کلید OpenAI

1. به [OpenAI Platform](https://platform.openai.com) بروید
2. وارد حساب کاربری شوید
3. به بخش API Keys بروید
4. یک کلید جدید ایجاد کنید
5. کلید را در فایل `.env` قرار دهید

## تست ربات

1. ربات را در تلگرام پیدا کنید (با username که تعریف کردید)
2. دستور `/start` را ارسال کنید
3. یک سوال بپرسید و پاسخ ربات را ببینید!

## دستورات مفید

- `/start` - شروع کار
- `/help` - راهنما
- `/profile` - نمایش پروفایل
- `/clear` - پاک کردن تاریخچه
- `/setcontext <متن>` - تنظیم زمینه گفتگو

